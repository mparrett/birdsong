[project]
name = "birdsong"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.1",
    "sounddevice>=0.5.2",
    "scipy>=1.11.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]
